# CLAUDE.md

Guidance for Claude Code working in this repository. **同一份文件可被 `AGENTS.md` 软链引用，Codex 也读它。**

> 本文件只放「LLM 不读就会出错」的硬约束。详细命令、配置项、代码示例去仓库源码或 `docs/` 看。

## 知识库与上下文获取规则

**Obsidian Vault**: `{{vault_path}}`
**本项目文档**: `项目/{{项目名}}/`（入口: `项目/{{项目名}}/概览.md`）
**首页**: `首页.md`（跨项目依赖图）
**关联项目**:
{{related_projects_block}}

## Obsidian 文档地图

<!-- 此区域由 /sync-docs 自动维护，不要手改。增删 Obsidian 文档时跑 /sync-docs 刷新。 -->

**项目入口 / 总览**
- `项目/{{项目名}}/概览.md` — 项目定位、技术栈、关键模块
- `项目/{{项目名}}/activeContext.md` — 当前焦点 + 最近改动（机器维护）
- `项目/{{项目名}}/progress.md` — 已完成功能 + 决策记录

**01-架构/**
- _（暂无文档，新增后跑 /sync-docs 自动刷新）_

**02-功能/**
- _（暂无文档）_

**03-产品/**
- _（暂无文档）_

**04-参考资料/**
- _（暂无文档）_

**05-测试与调试/**
- _（暂无文档）_

## 查阅优先级 + 触发规则清单

### 必须先读 Obsidian 的场景

- 用户问"X 是怎么实现/工作/设计的" → 查文档地图找对应 02-功能/ 或 01-架构/
- 用户说"加 / 改 / 重构 X 功能" → 动手前必读对应 02-功能/ 子文档
- 跨仓库任务（涉及关联项目）→ 先读 `首页.md` 和 `01-架构/跨项目交互图.md`（如有）
- 用户提"上次 / 之前 / 那个决策 / 历史问题" → 看 `progress.md` 决策记录或 `05-测试与调试/`
- 接触陌生模块 → 先看 `概览.md` 拿心智模型，再看子文档

### 可以跳过的场景

- 纯代码改动（typo / rename / 加日志 / 补注释）
- 用户给了完整上下文，没让你"理解"什么
- 只读查询（git log / grep / cat / ls）
- 用户明确说"直接看代码"

## Obsidian vs 代码的分工（地图 vs 地形）

**Obsidian 是地图，代码是地形**。地图建立心智模型，地形决定动手前的真相。

| 信息类型 | 真相源 | Obsidian 角色 |
|---------|--------|---------------|
| 代码现状（函数签名、调用链、实际行为） | 代码 | 缓存/索引，可能过期 |
| 设计意图 / 历史决策 / tradeoff | Obsidian | 唯一真相 |
| 当前焦点 / 进度 | Obsidian | 唯一真相 |
| 跨仓库依赖关系 | 两边核对 | 起点（声明），代码确认（实际 import） |

**改代码前必须 Read 实际文件**，不能凭 Obsidian 描述就改。

发现 Obsidian 与代码不一致时：
- 小偏差（命名、参数顺序、字段名）→ 立刻修 Obsidian
- 大偏差（架构、流程、模块拆分变了）→ 停下来告诉用户，问该改哪边
- **不要假设 Obsidian 对，也不要假设代码对**

## Git 操作约定（必须遵守）

**用户提到 commit / push / pull / 提交 / 推送 / 拉取时**：

- **优先用 `/commit` 和 `/pull`**，不要直接跑裸 `git commit` `git pull`
- 这两个 slash command 会自动同步 Obsidian 文档（activeContext / 首页 / 本仓库 CLAUDE.md "最近变更"），跳过会让文档和代码漂移
- 仅在以下场景才用裸 git：
  - 用户明确说"裸 git" / "不要走 skill"
  - 操作 skill 不支持（rebase / cherry-pick / reflog / bisect 等）
  - 只读查询（git log / git diff / git status 等）
- **裸 git 操作完成后**，主动建议用户跑 `/sync-docs` 补同步

---

## Project Overview

<!-- 一句话说清这个项目是什么、解决什么问题。50 字以内。 -->

**技术栈**: {{语言}} / {{框架/库}}

**主要模块**:
<!-- 列 3-5 个核心模块，详细架构去 Obsidian 01-架构/ -->

---

## 最近变更

> 此区域由 /sync-docs 自动更新，保留最近 5 次提交。**位置在文件最底部以保护 prefix cache。**

（暂无记录，下次 /sync-docs 后自动填充）
