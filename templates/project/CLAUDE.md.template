# CLAUDE.md

> 此文件是"路由表 + 硬约束"。**项目内容（架构、功能细节、API）全在 Obsidian**，按路由表按需查。

## 1. 元信息

**Obsidian Vault**: `{{vault_path}}`
**本项目文档**: `项目/{{项目名}}/`（入口: `项目/{{项目名}}/概览.md`）
**首页**: `首页.md`（跨项目依赖图）
**关联项目**:
{{related_projects_block}}

---

## 2. 项目速描

**做什么**：{{! 必填：谁在用、解决什么业务问题，不要只写技术栈 !}}
**技术形态**：{{架构类型 / 协议，一句话}}
**技术栈**：{{语言}} / {{框架/库}}
**核心模块**：
- `{{路径}}` — {{一句话说明}}

---

## 3. Obsidian 文档地图

<!-- 此区域由 /sync-docs 自动维护，不要手改。增删文档后跑 /sync-docs 刷新。 -->

**入口**
- `项目/{{项目名}}/概览.md` — 项目定位、技术栈、关键模块
- `项目/{{项目名}}/activeContext.md` — 当前焦点 + 最近改动（机器维护）
- `项目/{{项目名}}/progress.md` — 已完成功能 + 决策记录

**01-架构/**
- _（暂无文档，新增后跑 /sync-docs 刷新）_

**02-功能/**
- _（暂无文档）_

**03-产品/ 04-参考资料/ 05-测试与调试/**
- _（暂无文档）_

---

## 4. 路由表

> 拿到任务后先对号入座，再 Read 对应文档，然后看代码。**不要凭印象动手。**

| 用户意图 | 必读 Obsidian 文档 |
|---------|-----------------|
| "继续上次 / 上次做到哪 / 接着做" | `activeContext.md` 全文（当前焦点 + 最近变更）|
| "为什么最近 X 不对了 / 哪里改坏的" | `activeContext.md` 最近变更段 + `05-测试与调试/<bug 记录>` |
| 模糊排查（"X 坏了"但没指定文件）| `activeContext.md` 最近变更段 → 再 Read 代码 |
| "现在在做什么 / 进展" | `activeContext.md` 全文 |
| 接手项目 / 首次进入 | `概览.md` → `activeContext.md` |
| "X 是怎么实现/工作的" | `02-功能/<X>.md` |
| "为什么这么设计 / 历史决策" | `progress.md` 决策记录 + `02-功能/<X>.md` |
| 改 / 加 / 重构 X 功能 | `02-功能/<X>.md`（必读，再 Read 代码）|
| 跨仓库改动 | `首页.md` 依赖图 + 关联项目入口 |
| 用户指定了具体文件 / 行号 | ❌ 用户已给上下文，直接 Read 代码 |
| 纯代码改动（typo / rename / 加日志）| ❌ 不读 Obsidian |
| 只读查询（git log / grep / cat）| ❌ 不读 |

---

## 5. 真相源原则

**Obsidian 是地图，代码是地形。改代码前必须 Read 实际文件。**

| 信息类型 | 真相源 |
|---------|--------|
| 代码现状（函数签名、调用链、实际行为）| **代码** |
| 设计意图 / 历史决策 / tradeoff | **Obsidian** |
| 当前焦点 / 进度 | **Obsidian** |
| 跨仓库依赖 | 两边核对 |

发现不一致时：小偏差立刻修 Obsidian / 大偏差停下来问用户 / **不要假设哪边对**。

---

## 6. Git 操作约定

**用户提到 commit / push / pull / 提交 / 推送 / 拉取时**：

- **优先用 `/commit` 和 `/pull`**，不要直接跑裸 `git commit` `git pull`
- 这两个命令会自动同步 activeContext.md"最近变更"，跳过会让文档漂移
- 裸 git 仅限：用户明确说"裸 git"、skill 不支持的操作（rebase/cherry-pick 等）、只读查询
- **裸 git 完成后**主动建议：`"要不要跑 /sync-docs 补同步？"`

---

## 7. 项目硬约束

<!-- 只放"不每次提醒就会犯错"的强制规则。背景知识、架构描述请放 Obsidian。 -->

_（由 /add-project 或用户手动填写）_
