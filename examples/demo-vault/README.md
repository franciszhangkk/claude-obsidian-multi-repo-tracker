# Demo Vault

> ⚠️ **MVP 占位**：完整 demo vault 待补。

这里会放一个最小可用的示例 vault，让用户 5 分钟看明白结构和用法。

计划包含：

- 2 个示例项目（一个后端、一个前端，演示跨仓库依赖）
- 完整填写的 `首页.md` + 跨仓库依赖图
- 每个项目都有填好的 `概览.md` / `activeContext.md` / `progress.md`
- 几个示例的"已归档" claude-log 日志

完成 commands MVP 后再来填这块（demo 的内容应该贴近真实命令的产出）。
